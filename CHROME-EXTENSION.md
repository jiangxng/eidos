================================================================================
Chrome 插件：Eidos 对话归档助手 (Eidos Chat Archiver)
================================================================================

版本: 1.0.0
状态: 设计阶段
更新日期: 2026-09-02


1. 项目背景
================================================================================

在 Eidos 项目开发中，采用"主对话决策 + 子对话执行"的多对话协作模式。
但多对话之间存在信息孤岛，子对话的产出无法自动同步到主对话，导致：
- 手动复制粘贴效率低
- 容易遗漏关键信息
- 难以快速定位历史讨论内容

目标：开发一个 Chrome 插件，实现主子对话之间的信息通信和归档。


2. 核心需求
================================================================================

2.1 信息归档（子对话 → 归档库）

在 DeepSeek 聊天页面中，用户可以选择一段对话内容，点击"归档"按钮，
将信息保存到本地数据库。

归档信息包含：
- 内容 (content): 选中的文本
- 标签 (tags): 用户自定义标签（如 "bug修复"、"功能设计"）
- 时间戳 (timestamp): 归档时间
- 来源上下文 (context): 当前对话的主题（如 "子任务: TreeSelect组件"）
- 关联文件 (relatedFiles): 可选，关联的项目文件路径

2.2 信息浏览（归档库 → 主对话）

在 DeepSeek 页面中，用户可以通过侧边栏或弹出窗口浏览所有归档记录。

浏览功能支持：
- 按时间排序（最新优先）
- 按标签筛选
- 按关键词搜索

2.3 信息导入（归档库 → 主对话）

用户可以选择一条或多条归档记录，一键粘贴到当前对话输入框中，
作为上下文信息注入。

2.4 信息管理

用户可以对归档记录进行编辑、删除、打标签等管理操作。


3. 技术方案
================================================================================

3.1 技术栈

组件                     技术选型                                    说明
-------------------------------------------------------------------------------
插件框架                 Chrome Extension Manifest V3               最新标准
数据存储                 IndexedDB (via idb or Dexie)              本地持久化
前端框架                 原生 JS                                    轻量级
通信协议                 chrome.runtime API                        跨标签页通信
图标                     SVG / 字体图标                            轻量清晰

3.2 架构模块

1. Background Service Worker
   职责：管理数据存储、处理跨标签页消息
   文件：background.js
   核心接口：
   - saveRecord(data): 保存归档记录
   - getRecords(filter): 查询记录
   - deleteRecord(id): 删除记录
   - updateRecord(id, data): 更新记录

2. Content Script
   职责：在 DeepSeek 页面中注入 UI 元素（归档按钮、侧边栏）
   文件：content.js
   注入位置：
   - 对话气泡旁边 → 添加"归档"按钮
   - 页面右侧或底部 → 添加侧边栏/浮窗

3. Popup / Side Panel
   职责：提供主界面，用于浏览、搜索、管理归档记录
   文件：popup.html + popup.js
   触发方式：点击插件图标弹出

4. Options Page
   职责：插件设置（可选）
   文件：options.html + options.js

3.3 数据模型

Record 对象结构：
- id: 字符串，唯一标识 (uuid)
- content: 字符串，归档内容
- tags: 字符串数组，标签列表
- timestamp: 数字，归档时间戳
- context: 字符串，来源上下文
- relatedFiles: 字符串数组，关联文件路径
- sourceUrl: 字符串，来源 URL
- project: 字符串，项目名称 (默认: Eidos)

3.4 信息协议

子对话 → 归档库的消息格式：
{
  type: 'ARCHIVE',
  payload: {
    content: '选中的文本内容',
    tags: ['bug修复', '决策'],
    context: '子任务: TreeSelect 组件'
  }
}

归档库 → 主对话的导入格式：
{
  type: 'IMPORT_TO_CHAT',
  payload: {
    records: [ /* 选中的记录列表 */ ]
  }
}


4. 用户界面设计
================================================================================

4.1 归档按钮

- 位置：DeepSeek 每条消息的工具栏区域
- 样式：小图标按钮 (📎 或 📋)
- 交互：点击后弹出"归档"对话框，让用户输入标签

4.2 归档对话框

标题：📋 归档对话
字段：
- 内容 (预填选中的文本)
- 标签 (输入框，逗号分隔)
- 上下文 (预填当前会话标题)
按钮：确认归档 / 取消

4.3 侧边栏 (Sidebar)

- 触发：点击插件图标或右下角浮动按钮
- 布局：
  - 顶部: 搜索框 + 筛选按钮
  - 中间: 记录列表 (按时间倒序)
  - 底部: 导入选中记录到对话

4.4 Popup 弹窗

- 内容：快速浏览最近 10 条记录 + "打开完整侧边栏"按钮
- 设计：简洁，适合快速查阅


5. 开发步骤（建议顺序）
================================================================================

1. 项目初始化：创建 Chrome Extension 项目结构
2. 数据层 (IndexedDB)：实现存储、查询、删除、更新功能
3. Background Service Worker：实现消息路由
4. Content Script：注入归档按钮 + 归档对话框
5. Popup 界面：显示最近记录 + 搜索
6. 侧边栏界面：完整浏览 + 管理功能
7. 导入功能：从归档库导入到当前对话
8. 测试与打包：功能测试 + 打包发布


6. 项目结构
================================================================================

eidos-chat-archiver/
├── manifest.json           # 插件配置
├── background.js           # Service Worker
├── content.js              # Content Script
├── popup.html              # Popup 界面
├── popup.js
├── sidebar.html            # 侧边栏界面
├── sidebar.js
├── options.html            # 设置页面 (可选)
├── options.js
├── styles/
│   ├── popup.css
│   └── sidebar.css
├── icons/
│   ├── icon16.png
│   ├── icon48.png
│   └── icon128.png
└── lib/
    └── idb.js              # IndexedDB 封装


7. 关键技术点
================================================================================

1. IndexedDB 操作：使用 idb 或 Dexie 库简化操作
2. 跨标签页通信：使用 chrome.runtime.sendMessage 和 chrome.tabs.sendMessage
3. 内容注入：使用 MutationObserver 监听 DOM 变化，动态添加归档按钮
4. 权限最小化：仅申请必要的权限 (storage, activeTab, scripting)
5. 用户体验：操作反馈（成功/失败 Toast 提示）


8. 验收标准
================================================================================

- [ ] 可以在 DeepSeek 页面中选中文本并一键归档
- [ ] 归档记录在 IndexedDB 中持久化存储
- [ ] 侧边栏可以浏览所有归档记录（按时间倒序）
- [ ] 可以按标签筛选、按关键词搜索
- [ ] 可以选择记录并导入到当前对话
- [ ] 插件图标点击后弹出 Popup，显示最近记录
- [ ] 性能良好，1000+ 记录不影响使用


9. 子对话启动模板
================================================================================

如果你把这个文件发给子对话，请附上以下消息：

【Eidos 项目 - Chrome 插件开发子任务】

请阅读 CHROME-EXTENSION.md 完整需求和设计文档，然后开始开发。

任务: 开发 Eidos 对话归档助手 Chrome 插件

输出要求:
1. 完整的插件源码（按 CHROME-EXTENSION.md 中的项目结构）
2. 打包后的 .crx 文件（或发布到 Chrome 网上应用店）
3. 一份简短的 README.md（安装和使用说明）

如遇技术困难，请在完成报告末尾标注"需要主对话协助: [问题描述]"。


10. 扩展可能性（未来）
================================================================================

- 支持多个平台 (Claude, ChatGPT, 通义千问等)
- 云端同步 (通过 Chrome Storage Sync 或自建服务)
- AI 自动标签 (使用 DeepSeek API 自动为归档内容打标签)
- 团队共享 (多人协作的公共归档库)