# LLM Task Protocol
Before modifying canonical code:
1. Read `CONSTITUTION.md`.
2. Read `llm/repository-map.json`.
3. Read nearest capability/contract.
4. Prefer composition over new implementation.
5. Keep dependencies explicit and bounded.
6. Run `npm run typecheck`, `npm test`, `npm run validate:repo`.
