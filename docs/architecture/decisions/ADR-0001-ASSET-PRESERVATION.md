# ADR-0001 — Preserve implementation assets; converge architecture boundaries

Status: Proposed for acceptance by repository owner.

Decision: do not replace the existing Eidos repository with EIDOS-M0. Keep core/components/layout/data/auth/generator/playground/history and add versioned runtime boundaries around them. File moves are deferred until dependency ownership is proven.
