# Gap Analysis — Convergence Gates

P0: document authority, UIDL/version/schema boundary, fail-closed validation, legacy typecheck baseline, ActionRequest/execution isolation, no business-field inference.

P1: replaceable control registry, JSON wire boundary, deterministic environment snapshot, scoped event lifecycle, presentation-only policy, transport ports/response mapping, structured diagnostics, EVO/XC independence.

P2: feature flags/compatibility matrix/migrations/rollback, pure Node runtime entry and gray/shadow rollout without duplicate business execution.

Known existing behavior defects from the evidence report must be repaired independently of UIDL adoption so architecture change and bug fixes remain reviewable.
