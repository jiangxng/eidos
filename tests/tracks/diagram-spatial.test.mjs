import test from "node:test";import assert from "node:assert/strict";import {simpleFlowLayout} from "../../dist/diagram/editor.js";import {projectPerspective} from "../../dist/spatial/engine.js";test("diagram layouts and spatial projects",()=>{const d=simpleFlowLayout({contractVersion:"0.1.0",diagramId:"d",kind:"flowchart",editable:true,nodes:[{id:"a",kind:"x"},{id:"b",kind:"x"}],edges:[{id:"e",source:"a",target:"b"}]});assert.ok(d.nodes[1].x>d.nodes[0].x);assert.ok(Number.isFinite(projectPerspective({x:1,y:1,z:1}).x))});